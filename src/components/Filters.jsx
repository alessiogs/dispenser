import React from "react";

const Filters = ({ subjects, topics }) => {
  return (
    <div>
      <div>Filter by targer</div>
      <div>Filter by topic</div>
    </div>
  );
};

export default Filters;
